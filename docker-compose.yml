services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: berichtsheft
      POSTGRES_PASSWORD: berichtsheft
      POSTGRES_DB: berichtsheft
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
